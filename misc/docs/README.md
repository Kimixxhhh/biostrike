# 文档目录

本目录用于存放 BioStrike 项目的文档（`.md`）。

> create weapon scene
1 do'access as unique name'
2 add 'weapon data' at weapon scene inspector
3 override function status change/status cancel , selectThis/UnselectThis
